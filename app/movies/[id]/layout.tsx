import { ShellLayout } from "../../components/shared/sharedLayout";
import { createMetaData } from "../../utils/helper";

export const metadata = createMetaData({
  title: "Movie Details - Movar",
  description: "Details of a the movie",
});

export default ShellLayout;
